'use strict';

describe('s2n.viewTFavorites module', function() {

  beforeEach(module('sn2.viewFavorites'));

  describe('Favorites controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var FavoritesCtrl = $controller('FavoritesCtrl');
      expect(FavoritesCtrl).toBeDefined();
    }));

  });
});
