'use strict';

describe('s2n.viewTemplate module', function() {

  beforeEach(module('sn2.viewTemplate'));

  describe('Template controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var TemplateCtrl = $controller('TemplateCtrl');
      expect(AboutCtrl).toBeDefined();
    }));

  });
});